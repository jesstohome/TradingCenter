/superdad   admin  123456




nginx php7.3 mysql5.7 安装php扩展 fileinfo opcache redis imagemagick imap 并且禁用所有函数
nginx站点配置反向代理(代理目录/public)
        location ~/(wss|socket.io){ # 此处改为 socket.io 后端的 ip 和端⼝即可 
    proxy_pass http://127.0.0.1:2000; 
    proxy_set_header Upgrade $http_upgrade; 
    proxy_set_header Connection "upgrade"; 
    proxy_http_version 1.1; 
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; 
    proxy_set_header Host $host; 
    }

手机端nginx站点配置反向代理
      location ~/(api) {
# 此处改为 socket.io 后端的 ip 和端口即可 
    proxy_pass https://www.l-1.vip;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_http_version 1.1;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; 
  }

pc端nginx站点配置反向代理
          location ~/(api|upload) {
# 此处改为 socket.io 后端的 ip 和端口即可 
    proxy_pass https://www.l-1.vip;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_http_version 1.1;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; 
  }


放行所有端口
上传源码  设置public运行目录  laravel5伪静态   
 安装es  终端执行命令
1 yum install java -y

2(意思为先创建下方文件再往下执行安装)　
rpm --import https://artifacts.elastic.co/GPG-KEY-elasticsearch
vi /etc/yum.repos.d/elasticsearch.repo(没有此文件就创建)

3插入下面代码

[elasticsearch-7.x]
name=Elasticsearch repository for 7.x packages
baseurl=https://artifacts.elastic.co/packages/7.x/yum
gpgcheck=1
gpgkey=https://artifacts.elastic.co/GPG-KEY-elasticsearch
enabled=1
autorefresh=1
type=rpm-md

shift+：  输入wq回车保存 
4 yum install elasticsearch

5启动  
service elasticsearch start
6 检查是否启动
curl -X GET localhost:9200

切换到网站根目录  清理缓存（必须执行）  php artisan config:cache 
（必须设置redis密码并重启一切服务）

两个计划任务

1、start
cd /www/wwwroot/adminsup.cosimoc.com/public/vendor/webmsgsender 
php start.php start
2、  websocket:client
cd /www/wwwroot/adminsup.cosimoc.com
php artisan websocket:client restart

执行队列
cd /www/wwwroot/adminsup.cosimoc.com/
php artisan schedule:run
(如果报错设置密码重载redis配置)

队列
cd /www/wwwroot/adminsup.cosimoc.com/
php artisan queue:work

artisan 
cd /www/wwwroot/adminsup.cosimoc.com
php artisan robot 4



代理商后台地址 
https://admin.ncty888.com/agent
